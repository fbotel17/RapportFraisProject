<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Rapport Frais{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/historique.css') }}">

    <style>
        /* Styles pour la photo de fond sur mobile */
        @media (max-width: 768px) {
            body {
                background-image: url('{{ asset('images/camion.JPG') }}');
                background-size: cover;
                background-position: left;
                background-attachment: fixed;
                background-repeat: no-repeat;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Rapport Frais</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathinfo == path('ajouter_frais') %}active{% endif %}" href="{{ path('ajouter_frais') }}">Enregistrer une journée</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathinfo == path('statistiques') %}active{% endif %}" href="{{ path('statistiques') }}">Statistiques du mois</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathinfo == path('app_statistiques_globales') %}active{% endif %}" href="{{ path('app_statistiques_globales') }}">Statistiques Globales</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathinfo == path('historique') %}active{% endif %}" href="{{ path('historique') }}">Historique</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.pathinfo == path('admin') %}active{% endif %}" href="{{ path('admin') }}">Administration</a>
                    </li>
                    <!-- Ajoutez d'autres liens ici -->
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        {% block body %}{% endblock %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
